# Server env (do not commit real values)

# TypeSafe Jev — server-only. Never prefix with VITE_ (Vite would ship it in the browser bundle).
# Lovable: project Secrets / environment variables, name TYPESAFE_API_KEY.
# Cloudflare/Nitro: Worker secret / env binding with the same name.
# Local: copy this file to .env or .dev.vars (gitignored).
TYPESAFE_API_KEY=
